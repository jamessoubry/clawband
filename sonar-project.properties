# SonarQube Cloud project settings
# Fill in projectKey and organization after connecting the repo in SonarCloud UI
# (Settings → Administration → Projects → your project)
sonar.projectKey=REPLACE_WITH_PROJECT_KEY
sonar.organization=REPLACE_WITH_ORG_KEY

sonar.projectName=clawband
sonar.projectVersion=2.98.0

sonar.sources=src
sonar.exclusions=**/tests/**,**/*.md
